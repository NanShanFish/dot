#!/usr/bin/bash

if [ $# -eq 0 ]; then
    nvim
    exit
fi

if [ -e "$1" ]; then
    if [ -d "$1" ]; then
        cd "$1"
    else
        cd $(dirname "$1")
    fi
    nvim $(realpath "$@")
else
    nvim "$@"
fi
